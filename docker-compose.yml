services:
  postgres:
    image: postgres:17
    container_name: postgres-db
    restart: always

    environment:
      POSTGRES_USER: admin_devpulse
      POSTGRES_PASSWORD: 7a665gdfhjn
      POSTGRES_DB: devpulse_db

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: devpulse-redis
    restart: always

    ports:
      - "6379:6379"

volumes:
  postgres_data:
